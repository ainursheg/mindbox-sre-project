# === Шаг 1: Базовый образ ===
# Берем за основу официальный, очень легковесный образ Nginx
FROM nginx:alpine

# === Шаг 2: Копируем наше приложение ===
# Копируем нашу веб-страницу из папки src в рабочую директорию Nginx,
# откуда он раздает статические файлы.
COPY src/index.html /usr/share/nginx/html/index.html

# === Шаг 3: Информируем об используемом порте ===
# Сообщаем Docker, что контейнер будет слушать на порту 80
EXPOSE 80